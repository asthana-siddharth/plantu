PORT=5001

# Admin API token for simple auth middleware
ADMIN_TOKEN=admin123

# Can point to same DB host as app backend, or a dedicated admin DB user
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=root
DB_PASSWORD=root
DB_NAME=plantu_db
DB_POOL_SIZE=10
